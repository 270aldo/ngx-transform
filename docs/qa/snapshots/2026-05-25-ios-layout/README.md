# 2026-05-25 iOS Layout Pass

Scope:

- Reworked `LandingHowItWorks` from a repeated card grid into a split editorial layout with an iOS-style grouped row panel.
- Reworked `LandingValueStack` into the same split layout pattern, keeping the product micro-mocks as trailing row content.
- Kept the restored NGX color system intact; this pass was layout/composition only.

Reference direction:

- Mobbin web references: premium product pages using large editorial headings, grouped content blocks, wide spacing, and restrained hairline dividers.
- Mobbin iOS references: Fitbod, WHOOP, and Peloton-style grouped rows with clear row anatomy and trailing values/content.

Captured views:

- `layout-ios-how-it-works-desktop.png`
- `layout-ios-how-it-works-mobile.png`
- `layout-ios-value-stack-desktop.png`
- `layout-ios-value-stack-mobile.png`
