# 2026-05-25 Report Preview Layout Fix

Scope:

- Reworked `LandingReportPreview` desktop structure from a double split layout into one stable composition.
- Left side now holds section label, headline, explanation, bullets, and CTA as one narrative column.
- Right side now uses one grouped report panel with score, bottleneck, palancas, error, and roadmap sections.
- Kept the existing NGX color language intact.

Captured views:

- `report-preview-layout-fixed-desktop.png`
- `report-preview-layout-fixed-mobile.png`
